const fullWidthSlices = 'full-bleed-image, change-background-color';
const defaultProps = {
  primary: {
    type: Object,
    default: null,
  },
  items: {
    type: Array,
    default: null,
  },
};

export default { fullWidthSlices, defaultProps };
